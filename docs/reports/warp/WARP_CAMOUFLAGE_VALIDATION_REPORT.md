# WARP-C10 — camouflage target validation

Unit coverage now includes candidate budgets, cutoff generation/order, outer/inner isolation, RST observe-only behavior, route/path identity, trace redaction/completeness, and cleanup ownership. The required target matrix remains canonical/cover/split/fake, segmented/GSO parity, WAN flap/reconnect, nested validation, passive-RST canary, and rollback from every candidate phase.

Physical Keenetic/Android target execution is not available in this environment. The correct verdict is `BLOCKED_TARGET_VALIDATION`, not a simulated PASS. No missing physical evidence is converted into a production promotion.

