# SPF-8 — Rollback and false-positive budget

Status: IMPLEMENTED_NOT_TARGET_VALIDATED

Rollback consumes the scoped lease and auto-degrades policy to observe when the
configured false-positive budget is exhausted.
