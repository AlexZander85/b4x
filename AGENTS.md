# AGENTS.md — b4x (читать до любого деплоя на роутер)

Живой Keenetic. Ошибочный деплой роняет YouTube у владельца.

## Что открыть

1. `PROJECT_DIRECTIVES.md` — истина: стек, запреты, лестница, путь к продукту.
2. `YOUTUBE_DATAPLANE.md` — карта YouTube: ТСПУ, три тактики, лестница seek, что не открывать.
3. `.ag/summaries/state_packet.md` — только открытый слой и отпечатки.

Не открывать как план: `B4X_HARDWARE_FIELD_VALIDATION_AGENT_INSTRUCTION.md`, `.ag/findings.md`, архив `session_summary`, addenda WARP/ABD — пока фаза не P1+.

Отвечать владельцу по-русски.

## Жёсткие правила

- Один слой лестницы на сессию. Слой берётся из `state_packet`. Seek: не ips/syn_fake; лестница и тактики A/B/C — `YOUTUBE_DATAPLANE.md`.
- Не перезаписывать живые `$F/bin/b4` и `/opt/etc/b4/b4.json`. Только копии `b4.exp-<слой>` на **флешке**. `/opt/sbin/b4` — один live-бинарь (USB бывает noexec). Не складывать архив `.exp-*` в `/opt` (98 МБ).
- Не деплоить `30d4208b` и `43b5042b` (FD-утечка).
- Не ставить `offload_policy=exclude`.
- Не включать `sni_mutation` / substitute на проде. Тикет `b4x-smo` закрыт: ломает TLS 1.3 у ванили.
- Не менять в одном деплое бинарь + PPE + конфиг сетов + логи.
- Вердикт UX — только слова владельца про `.152` (ваниль / ReVanced / seek / Gmail). curl с `.40` — вспомогательный.
- Не запускать кампанию L0–L8 field pack, пока владелец не скажет «фаза P3».
- Не клеймить `b4x-smo`, `b4x-a3i`, `b4x-101`, пока не PASS слой L4.

`$F` = `/tmp/mnt/37fdc502-d92b-dd01-3075-c402d92bdd01`

## Стек (не путать цель и факт)

| | Сейчас на роутере | Цель |
|---|---|---|
| Бинарь / конфиг / PPE | **только** `state_packet` (память чата врёт) | HEAD после L2, без регресса |
| Fake SNI | AutoTTL; короткий CH = combo; ECH = hold + 2–3 сегмента | доказать pcap'ом; badsum — отдельный прогон |
| Логи | `$F/b4/log/b4.log` | тот же + GUI без FD-утечки |

Продукт целиком: `monitor → ABD → discovery → canary`. YouTube — полигон этого цикла. Руками стратегии не подбирать.

## Старт сессии

1. Чеклист L0 из `PROJECT_DIRECTIVES.md` на **живом** роутере (память чата протухла).
2. Прочитать открытый слой в `state_packet`.
3. `bd ready --json` — брать только задачу текущего слоя.
4. Первое сообщение владельцу: fd / sha / md5 / какой слой делаешь. Если L0 красный — стоп.

## Конец сессии (обязательно)

Переписать **первые 15 строк** `.ag/summaries/state_packet.md`: слой, sha, md5, PASS/FAIL, следующий слой. Иначе следующая сессия начнёт день сурка.

Не коммитить и не пушить без прямой просьбы.
